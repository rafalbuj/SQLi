SQLi Python script to search for dorks, go to the websites containing the dorks and check if they're SQL vulnerable. 
To run the script, just use: python sqli
You can search with a custom dork of your choice or choose option 2 for dictionary which will search through a list of dorks. 

